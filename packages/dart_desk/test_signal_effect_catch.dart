import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:dart_desk/src/studio/screens/document_list.dart';

void main() {
  testWidgets('Catch', (tester) async {
     // I just need to modify `_DocumentStatusPill.build` to catch it temporarily!
  });
}
